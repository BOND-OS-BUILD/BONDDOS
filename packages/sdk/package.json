{
  "name": "@bond-os/sdk",
  "version": "1.0.0",
  "private": true,
  "description": "Official Extension SDK for BOND OS — a typed client for the public API, webhook signature verification, and a typed event router. Zero runtime dependencies.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@bond-os/config": "workspace:*",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2"
  }
}
