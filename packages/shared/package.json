{
  "name": "@bond-os/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-cutting infrastructure shared by every BOND OS package/app: env validation, logging, error types, cache, rate limiting, and zod schemas.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.4.2",
    "pino": "^9.6.0",
    "server-only": "^0.0.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@bond-os/config": "workspace:*",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "pino-pretty": "^13.0.0",
    "typescript": "^5.7.2"
  }
}
